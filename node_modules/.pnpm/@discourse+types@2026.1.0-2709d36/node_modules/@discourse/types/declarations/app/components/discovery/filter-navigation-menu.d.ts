/**
 * FilterNavigationMenu - A simpler UI component for filter input and suggestions
 *
 * This component manages:
 * - User input field
 * - Keyboard navigation
 * - Dropdown menu display
 * - Selection handling
 *
 * The actual suggestion generation is delegated to FilterSuggestions
 */
export default class FilterNavigationMenu extends Component<any> {
    constructor(owner: import("@ember/-internals/owner").default, args: any);
    menu: any;
    site: any;
    currentInputValue: any;
    suggestions: any[];
    activeFilter: any;
    lastSuggestionInput: string;
    suggestionRequestId: number;
    trackedMenuListData: {
        suggestions: any[];
        selectedIndex: any;
        selectItem: (item: any) => Promise<void>;
    };
    searchTimer: any;
    inputElement: any;
    dMenuInstance: any;
    _selectedIndex: number;
    set selectedIndex(value: number);
    get selectedIndex(): number;
    clearSelection(): void;
    get nothingSelected(): boolean;
    storeInputElement(element: any): void;
    updateSuggestions(): Promise<void>;
    fetchSuggestions(): Promise<void>;
    ensureFreshSuggestions(): Promise<void>;
    selectItem(item: any): Promise<void>;
    updateInput(value: any, submitQuery?: boolean): Promise<void>;
    clearInput(): Promise<void>;
    openFilterMenu(): Promise<void>;
    handleKeydown(event: any): Promise<void>;
    navigateDown(): void;
    navigateUp(): void;
    selectCurrent(): void;
    submitQuery(): Promise<void>;
    syncFromInitialValue(): void;
}
import Component from "@glimmer/component";
//# sourceMappingURL=filter-navigation-menu.d.ts.map