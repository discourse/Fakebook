/**
   A plugin outlet is an extension point for templates where other templates can
   be inserted by plugins.

   ## Usage

   If your handlebars template has:

   ```handlebars
     <PluginOutlet @name="evil-trout" />
   ```

   Then any handlebars files you create in the `connectors/evil-trout` directory
   will automatically be appended. For example:

   plugins/hello/assets/javascripts/discourse/templates/connectors/evil-trout/hello.hbs

   With the contents:

   ```handlebars
     <b>Hello World</b>
   ```

   Will insert <b>Hello World</b> at that point in the template.

**/
export default class PluginOutletComponent extends Component<any> {
    constructor(...args: any[]);
    clientErrorHandler: any;
    context: any;
    getConnectors({ hasBlock }?: {}): any;
    connectorsExist({ hasBlock }?: {}): boolean;
    get outletArgs(): any;
    get outletArgsWithDeprecations(): any;
    safeCurryComponent(component: any, args: any): any;
}
import Component from "@glimmer/component";
//# sourceMappingURL=plugin-outlet.d.ts.map