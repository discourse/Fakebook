/**
 * Helper class for managing bulk selection of posts
 * Similar to BulkSelectHelper but specialized for posts
 *
 * Features:
 * - Individual post selection/deselection
 * - Select All / Clear All functionality
 * - Shift+click range selection (like topic lists)
 * - Bulk action execution
 */
export default class PostBulkSelectHelper {
    constructor(context: any, posts?: any);
    loading: boolean;
    lastClickedPost: any;
    bulkSelectEnabled: boolean;
    posts: any;
    selected: TrackedArray<unknown>;
    /**
     * Update posts and clean up stale selections
     * @param {Array} newPosts - New posts array
     */
    updatePosts(newPosts: any[]): void;
    get selectedCount(): number;
    get hasSelection(): boolean;
    get allSelected(): boolean;
    selectPost(post: any): void;
    deselectPost(post: any): void;
    togglePost(post: any, options?: {}): void;
    selectRange(startPost: any, endPost: any): void;
    selectAll(): void;
    clearAll(): void;
    toggleBulkSelect(event: any): void;
    clear(): void;
    isSelected(post: any): boolean;
    getPostId(post: any): any;
    performBulkAction(actionFn: any): Promise<void>;
    #private;
}
import { TrackedArray } from "@ember-compat/tracked-built-ins";
//# sourceMappingURL=post-bulk-select-helper.d.ts.map