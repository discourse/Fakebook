export default class Users extends DiscourseRoute {
    router: any;
    siteSettings: any;
    queryParams: {
        period: {
            refreshModel: boolean;
        };
        order: {
            refreshModel: boolean;
        };
        asc: {
            refreshModel: boolean;
        };
        name: {
            refreshModel: boolean;
            replace: boolean;
        };
        group: {
            refreshModel: boolean;
        };
        exclude_groups: {
            refreshModel: boolean;
        };
        exclude_usernames: {
            refreshModel: boolean;
        };
    };
    titleToken(): any;
    resetController(controller: any, isExiting: any): void;
    beforeModel(): void;
    model(params: any): globalThis.Promise<{
        params: any;
        columns: any;
    }>;
    columns: any;
    setupController(controller: any, { columns, params }: {
        columns: any;
        params: any;
    }): import("rsvp").default.Promise<any[]>;
}
import DiscourseRoute from "discourse/routes/discourse";
//# sourceMappingURL=users.d.ts.map