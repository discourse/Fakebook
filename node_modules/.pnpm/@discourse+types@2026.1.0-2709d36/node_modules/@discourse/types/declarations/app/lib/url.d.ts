export function rewritePath(path: any): any;
export function clearRewrites(): void;
export function userPath(subPath: any): any;
export function groupPath(subPath: any): any;
export function setURLContainer(container: any): void;
export function prefixProtocol(url: any): any;
export function getCategoryAndTagUrl(category: any, subcategories: any, tagName: any): any;
export function getEditCategoryUrl(category: any, subcategories: any, tab: any): any;
export function getCanonicalUrl(absoluteUrl: any): string;
export const TOPIC_URL_REGEXP: RegExp;
export default _urlInstance;
declare let _urlInstance: DiscourseURL;
declare class DiscourseURL extends EmberObject {
    isJumpScheduled(): boolean;
    jumpToPost(postNumber: any, opts: any): void;
    replaceState(path: any): void;
    pushState(path: any): void;
    routeToTag(a: any): any;
    /**
      Our custom routeTo method is used to intelligently overwrite default routing
      behavior.
  
      It contains the logic necessary to route within a topic using replaceState to
      keep the history intact.
    **/
    routeTo(path: any, opts: any): any;
    routeToUrl(url: any, opts?: {}): void;
    rewrite(regexp: any, replacement: any, opts: any): void;
    redirectAbsolute(url: any): boolean;
    redirectTo(url: any): boolean;
    isInternal(url: any): any;
    isInternalTopic(url: any): any;
    /**
      If the URL is in the topic form, /t/something/:topic_id/:post_number
      then we want to apply some special logic. If the post_number changes within the
      same topic, use replaceState and instruct our controller to load more posts.
    **/
    navigatedToPost(oldPath: any, path: any, routeOpts: any): boolean;
    /**
      @private
  
      Handle the custom case of routing to the root path from itself.
  
      @param {String} oldPath the previous path we were on
      @param {String} path the path we're navigating to
    **/
    private refreshedHomepage;
    get origin(): string;
    get isComposerOpen(): any;
    get router(): any;
    get routerService(): any;
    get appEvents(): any;
    controllerFor(name: any): any;
    /**
      Be wary of looking up the router. In this case, we have links in our
      HTML, say form compiled markdown posts, that need to be routed.
    **/
    handleURL(path: any, opts: any): any;
    jumpToElement(elementId: any): void;
}
import EmberObject from "@ember/object";
//# sourceMappingURL=url.d.ts.map