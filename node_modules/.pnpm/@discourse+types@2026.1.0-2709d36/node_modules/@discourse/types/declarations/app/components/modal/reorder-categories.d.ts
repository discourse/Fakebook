export default class ReorderCategories extends Component<any> {
    constructor(owner: import("@ember/-internals/owner").default, args: any);
    site: any;
    changed: boolean;
    entries: any;
    highlightedCategoryId: any;
    get sortedEntries(): any;
    reorder(from: any): any;
    /**
     * 1. Make sure all categories have unique position numbers.
     * 2. Place sub-categories after their parent categories while maintaining
     *    the same relative order.
     *
     *    e.g.
     *      parent/c2/c1          parent
     *      parent/c1             parent/c1
     *      parent          =>    parent/c2
     *      other                 parent/c2/c1
     *      parent/c2             other
     **/
    createEntries(from: any, position?: number, categoryId?: any, depth?: number): any;
    move(entry: any, delta: any): void;
    toggleHighlight(categoryId: any): void;
    change(entry: any, newPosition: any): void;
    save(): Promise<void>;
}
import Component from "@glimmer/component";
//# sourceMappingURL=reorder-categories.d.ts.map