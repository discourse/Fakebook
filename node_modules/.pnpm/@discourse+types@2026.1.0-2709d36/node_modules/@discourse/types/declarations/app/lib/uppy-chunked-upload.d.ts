/**
 * Used mainly as a replacement for Resumable.js, using code cribbed from
 * uppy's S3 Multipart class, which we mainly use the chunking algorithm
 * and retry/abort functions of. The _buildFormData function is the one
 * which shapes the data into the same parameters as Resumable.js used.
 *
 * See the UppyChunkedUploader class for the uppy uploader plugin which
 * uses UppyChunkedUpload.
 */
export default class UppyChunkedUpload {
    constructor(file: any, options: any);
    options: any;
    file: any;
    chunkSize: any;
    abortController: AbortController;
    _aborted(): boolean;
    _initChunks(): void;
    chunksInProgress: number;
    chunks: any[];
    chunkState: {
        bytesUploaded: number;
        busy: boolean;
        done: boolean;
    }[];
    _createUpload(): void;
    _uploadChunks(): void;
    _shouldRetry(err: any): boolean;
    _retryable({ before, attempt, after }: {
        before: any;
        attempt: any;
        after: any;
    }): any;
    _uploadChunkRetryable(index: any): any;
    _uploadChunk(index: any): import("rsvp").default.Promise<any>;
    _onChunkProgress(index: any, sent: any): void;
    _onChunkComplete(index: any): void;
    _uploadChunkBytes(index: any, url: any, headers: any): import("rsvp").default.Promise<any>;
    _completeUpload(): globalThis.Promise<void>;
    _buildFormData(currentChunkNumber: any, body: any): FormData;
    _abortUpload(): void;
    _onError(err: any): void;
    start(): void;
    abort(opts?: any): void;
}
//# sourceMappingURL=uppy-chunked-upload.d.ts.map