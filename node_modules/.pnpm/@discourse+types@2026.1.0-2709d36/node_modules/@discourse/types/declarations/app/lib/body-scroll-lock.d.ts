/**
 * clear all body locks
 * @param options
 */
export function clearBodyLocks(options: any): void;
/**
 * get current lockState
 * @param options
 * @returns lockState
 */
export function getLockState(options: any): {
    lockedNum: number;
    lockedElements: any[];
    unLockCallback: any;
    documentListenerAdded: boolean;
    initialClientPos: {
        clientX: number;
        clientY: number;
    };
};
export namespace getLockState {
    export { initialLockState as lockState };
}
/**
 * lock body scroll
 * @param targetElement the element(s) still needs scrolling（iOS only）
 * @param options
 */
export function lock(targetElement: any, options: any): void;
/**
 * unlock body scroll
 * @param targetElement the element(s) still needs scrolling（iOS only）
 * @param options
 */
export function unlock(targetElement: any, options: any): void;
declare namespace initialLockState {
    let lockedNum: number;
    let lockedElements: any[];
    let unLockCallback: any;
    let documentListenerAdded: boolean;
    namespace initialClientPos {
        let clientX: number;
        let clientY: number;
    }
}
export {};
//# sourceMappingURL=body-scroll-lock.d.ts.map