export default class ActionSummary extends RestModel {
    canToggle: any;
    removeAction(): void;
    togglePromise(post: any): Promise<{
        acted: boolean;
    }> | Promise<void | {
        acted: boolean;
        remaining: number;
        max: number;
    }>;
    toggle(post: any): boolean;
    act(post: any, opts: any): Promise<void | {
        acted: boolean;
        remaining: number;
        max: number;
    }>;
    undo(post: any): Promise<{
        acted: boolean;
    }>;
}
import RestModel from "discourse/models/rest";
//# sourceMappingURL=action-summary.d.ts.map