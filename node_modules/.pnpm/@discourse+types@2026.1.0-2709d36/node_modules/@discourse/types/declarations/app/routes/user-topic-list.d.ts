export namespace QUERY_PARAMS {
    namespace ascending {
        export let replace: boolean;
        export let refreshModel: boolean;
        let _default: boolean;
        export { _default as default };
    }
    namespace order {
        let replace_1: boolean;
        export { replace_1 as replace };
        let refreshModel_1: boolean;
        export { refreshModel_1 as refreshModel };
    }
}
export default class UserTopicsListRoute extends DiscourseRoute {
    queryParams: {
        ascending: {
            replace: boolean;
            refreshModel: boolean;
            default: boolean;
        };
        order: {
            replace: boolean;
            refreshModel: boolean;
        };
    };
    setupController(controller: any, model: any): void;
}
import DiscourseRoute from "discourse/routes/discourse";
//# sourceMappingURL=user-topic-list.d.ts.map