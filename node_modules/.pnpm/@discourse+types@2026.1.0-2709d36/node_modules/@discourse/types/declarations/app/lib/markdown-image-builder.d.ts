export function sanitizeAlt(text: any, options?: {}): any;
/**
 * Extracts the extension (without dot) from a URL or path.
 * Returns null when no extension is present.
 *
 * @param {string} url
 * @returns {string|null}
 */
export function extensionFromUrl(url: string): string | null;
export function buildImageMarkdown(imageData: any): string;
//# sourceMappingURL=markdown-image-builder.d.ts.map