declare namespace _default {
    let data: Map<any, any>;
    function store(key: any, value: any): void;
    /**
      To retrieve a key, you provide the key you want, plus a finder to load
      it if the key cannot be found. Once the key is used once, it is removed
      from the store.
      So, for example, you can't load a preloaded topic more than once.
    **/
    function getAndRemove(key: any, finder: any): import("rsvp").default.Promise<any>;
    function has(key: any): boolean;
    function get(key: any): any;
    function remove(key: any): void;
    function reset(): void;
}
export default _default;
//# sourceMappingURL=preload-store.d.ts.map