export function addLogSearchLinkClickedCallbacks(fn: any): void;
export function resetLogSearchLinkClickedCallbacks(): void;
export function addSearchResultsCallback(callback: any): void;
export function translateResults(results: any, opts: any): any;
export function searchForTerm(term: any, opts: any): globalThis.Promise<any>;
export function searchContextDescription(type: any, name: any): any;
export function getSearchKey(args: any): string;
export function isValidSearchTerm(searchTerm: any, siteSettings: any): boolean;
export function applySearchAutocomplete(inputElement: any, siteSettings: any, owner: any): void;
export function updateRecentSearches(currentUser: any, term: any): void;
export function logSearchLinkClick(params: any): void;
/**
 * reciprocallyRankedList() makes use of the Reciprocal Ranking Fusion Algorithm (RRF)
 *
 * A method used to combine rankings from multiple sources
 * to aggregate them to provide a single improved ranking
 *
 * RRF = 1 / k + r(d)
 *
 * k = a constant, small positive value to avoid division by zero
 * r(d) = the reciprocal rank of the item in the ranking list
 *
 *
 * @param {Array} lists - an array of arrays containing the results from each source
 * The passed-in list must include the properties specified in the `identifiers` array
 * @param {Array} identifiers - an array of property names used to identify items in the ranking lists
 *
 * Example Usage: reciprocallyRankedList([list1, list2, list3], ["id", "topic_id", "uuid"])
 *
 **/
export function reciprocallyRankedList(lists: any[], identifiers: any[]): any[];
//# sourceMappingURL=search.d.ts.map