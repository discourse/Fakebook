export default class PostReplyAdapter extends RestAdapter {
    find(_store: any, _type: any, { postId, after }: {
        postId: any;
        after?: number;
    }): Promise<{
        post_replies: any;
    }>;
}
import RestAdapter from "discourse/adapters/rest";
//# sourceMappingURL=post-reply.d.ts.map