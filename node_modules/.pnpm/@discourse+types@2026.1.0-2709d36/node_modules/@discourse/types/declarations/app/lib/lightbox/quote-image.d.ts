/**
 * Checks if the slide element and data can be used to build a quote.
 *
 * @param {Element} slideElement
 * @param {object} slideData
 * @returns {boolean}
 */
export function canBuildImageQuote(slideElement: Element, slideData: object): boolean;
export default function quoteImage(slideElement: any, slideData: any): Promise<boolean>;
//# sourceMappingURL=quote-image.d.ts.map