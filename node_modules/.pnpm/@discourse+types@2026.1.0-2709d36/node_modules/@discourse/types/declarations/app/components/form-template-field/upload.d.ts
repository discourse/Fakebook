export default class FormTemplateFieldUpload extends Component<any> {
    constructor(owner: import("@ember/owner").default, args: any);
    uploadValue: any;
    fileInputSelector: string;
    fileUploadElementId: string;
    uploadedFiles: any[];
    uppyUpload: UppyUpload;
    get uploadStatusLabel(): "form_templates.upload_field.uploading" | "form_templates.upload_field.upload";
    get disabled(): boolean;
    /**
     * The validation from PickFilesButton._filesPicked, where acceptedFormatsOverride
     * is validated and displays a message, happens after the upload is complete.
     *
     * Overriding this method allows us to validate the file before the upload
     *
     * @param file
     * @returns {boolean}
     */
    isUploadedFileAllowed(file: any): boolean;
    uploadDone(upload: any): void;
    buildMarkdown(upload: any): string;
}
import Component from "@glimmer/component";
import UppyUpload from "discourse/lib/uppy/uppy-upload";
//# sourceMappingURL=upload.d.ts.map