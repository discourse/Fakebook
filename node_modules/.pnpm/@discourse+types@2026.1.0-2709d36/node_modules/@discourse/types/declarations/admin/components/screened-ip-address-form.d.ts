/**
  A form to create an IP address that will be blocked or allowed.
  Example usage:

    {{screened-ip-address-form action=(action "recordAdded")}}

  where action is a callback on the controller or route that will get called after
  the new record is successfully saved. It is called with the new ScreenedIpAddress record
  as an argument.
**/
export default class ScreenedIpAddressForm extends Component<any> {
    constructor(owner?: import("@ember/-internals/owner").default | undefined);
    dialog: any;
    formSubmitted: boolean;
    actionName: string;
    actionNames(adminAllowlistEnabled: any): {
        id: string;
        name: any;
    }[];
    focusInput(): void;
    submitForm(): void;
}
import Component from "@ember/component";
//# sourceMappingURL=screened-ip-address-form.d.ts.map