import { LanguagePlugin } from '@volar/language-core';
import type ts from 'typescript';
import { URI } from 'vscode-uri';
import { GlintConfig } from '../index.js';
export type TS = typeof ts;
/**
 * Create a [Volar](https://volarjs.dev) language plugin to support
 *
 * - .gts/.gjs files (the `ember-template-imports` environment)
 */
export declare function createEmberLanguagePlugin<T extends URI | string>(glintConfig: GlintConfig, { clientId }?: {
    clientId?: string;
}): LanguagePlugin<T>;
//# sourceMappingURL=ember-language-plugin.d.ts.map